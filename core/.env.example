# ====================================================================
# Clinical Data Generator - Environment Configuration Example
# ====================================================================
# Copy this file to: cred/.env (remove the .example extension)
# Then fill in your actual credentials below.

# --------------------------------------------------------------------
# AI Provider Selection
# --------------------------------------------------------------------
# Choose which AI service to use for text/image generation
# Options: "openai" or "vertexai"
LLM_PROVIDER=openai

# --------------------------------------------------------------------
# OpenAI Configuration (if using OpenAI)
# --------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# --------------------------------------------------------------------
# Google Cloud / Vertex AI Configuration (if using Vertex AI)
# --------------------------------------------------------------------
# Your Google Cloud Project ID
GCP_PROJECT_ID=your-gcp-project-id

# Region for Vertex AI (e.g., us-central1, us-east1)
# Note: Use region names, not zones (e.g., "us-central1" not "us-central1-a")
GCP_LOCATION=us-central1

# Path to your service account JSON key file
# Save your key file in cred/ folder and reference it here
GOOGLE_APPLICATION_CREDENTIALS=./cred/gcp_auth_key.json

# --------------------------------------------------------------------
# Application Settings
# --------------------------------------------------------------------
# Test mode uses cheaper/faster models (gpt-4o-mini, gemini-2.5-flash)
# Set to "true" for testing, "false" for production quality
TEST_MODE=false

# Directory where generated PDFs and images will be saved
# Default: generated_output (relative to project root)
OUTPUT_DIR=generated_output

# --------------------------------------------------------------------
# Advanced Settings (Optional)
# --------------------------------------------------------------------
# Add any custom environment variables below as needed

# ============================================================
# OPTIONAL: Web Search for Medical Information
# ============================================================
# Enable web search to retrieve precise CPT/ICD descriptions and policy criteria
# from authoritative sources (AAPC, CMS, insurance payer websites)
ENABLE_WEB_SEARCH=false

# Tavily API Key for web search (get free key at https://tavily.com)
# Free tier: 1,000 searches/month | Pro tier: $0.005 per search
TAVILY_API_KEY=your_tavily_api_key_here

# Search cache TTL in hours (default: 24)
SEARCH_CACHE_TTL=24
